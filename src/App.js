import React, { Component } from 'react';
import logo from './logo.svg';
import './App.css';
import { primary } from 'util/colors';

class App extends Component {
  render() {
    return (
      <div style={{ position: "fixed", top: 0, left: 0, right: 0, bottom: "100%", backgroundColor: "red" }}>
      </div>
    );
  }
}

export default App;
